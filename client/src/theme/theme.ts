import { createTheme } from '@mui/material';

// default tema.
const theme = createTheme({
    palette: {
      primary: {
        main: '#006064',
      },
    },
  zIndex: {
    appBar: 1250,
  },
});

export default theme;
